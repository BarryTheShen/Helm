# Traceability Matrix

Cross-reference: REQ-ID → source → implementation → test → verdict.

**Status:** Template — not yet populated.

Populate by running `helm-requirements-auditor` (for source → REQ-ID mapping) and `helm-reviewer` (for implementation → test → verdict tracking).

| REQ ID | Source Document | Implementation Evidence | QA Evidence | Verdict |
|--------|----------------|------------------------|-------------|---------|
| <!-- REQ-ID --> | <!-- Source doc reference --> | <!-- File paths, commit hashes --> | <!-- Test names, manual test results --> | <!-- PASS / FAIL / PARTIAL / NOT TESTED --> |
