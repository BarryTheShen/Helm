# Requirements Ledger

Atomic requirements table with full traceability from source document to REQ-ID.

**Status:** Populated

| REQ ID | Source doc/page | Source section/path | Context notes | Expanded contextual requirement | Type | Priority | Acceptance criteria | QA mode | Slice ID |
|--------|-----------------|---------------------|---------------|-------------------------------|------|----------|-------------------|---------|----------|
| **App Editor Requirements** |
| FF4-APP-001 | FF4 doc lines 7-15 | Helm Admin / App Editor | Overall UI not displaying properly with icon issues throughout | The App Editor must allow setting icons for individual modules (Home, Chat, etc.). Icons must be configurable per-module, not blank space. | UI | must | Given a module in the App Editor, when the admin clicks the module's icon area, then an icon picker opens allowing selection; Given a module with an assigned icon, when the app is previewed or published, then the icon renders correctly (not blank). | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-002 | FF4 doc lines 11-12 | App Editor / push to sync | Settings must push changes to backend JSON; save must cause visible change on frontend | The App Editor must push all edits to the backend JSON that is the source of truth. When the user saves, changes must be reflected by the backend serving updated JSON. There must be bidirectional sync between admin edits and the backend-served mobile JSON. | functional | must | Given a change in App Editor settings (module enable/disable, icon, bottom bar config), when the user saves, then the backend stores the change; Given the backend has updated app config, when the mobile app fetches its assigned app, then it receives the latest config. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-003 | FF4 doc lines 13-14 | App Editor / mobile UI simplification | Simplify mobile UI to single phone section | The App Editor mobile preview must show a simplified phone layout: a center-screen area (Launchpad preview showing module icons) and a bottom bar. No two sidebars. The center shows Launchpad icons that can be dragged to the bottom bar. There is one additional sidebar for module-specific settings. | UI | must | Given the App Editor is open, when viewing the phone preview area, then only one center screen and one bottom bar are visible (no left/right sidebars within the phone mockup); Given a module icon in the center Launchpad area, when the admin drags it to the bottom bar, then it appears in the bottom bar. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-004 | FF4 doc lines 15-16 | App Editor / dark mode, live testing | Dark mode does not sync to mobile; live testing is not done correctly | The App Editor's dark mode setting must correctly sync and render on the mobile side. All App Editor settings must be verified through live testing. | functional | must | Given the App Editor sets dark mode to on, when the mobile app receives the published app version, then the app renders in dark mode; Given any change is made in the App Editor, when the mobile app receives the published version, then the mobile app reflects all changes accurately. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-005 | FF4 doc lines 586-612 | Delivery/Versioning / Core principle | Mobile app must not receive random live mutations; renders a selected App Version from App Editor | The mobile app must render a selected App Version produced by the App Editor. The Module Editor edits modules like a document and can preview modules inside the web admin, but never pushes directly to mobile. Mobile preview and mobile publish always originate from the App Editor. | architecture | must | Given the Module Editor saves a module draft, then the mobile app does not change; Given the App Editor publishes a version, then the mobile app updates to the published version; Given a preview is started from the Module Editor, then it is web-admin-only. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-006 | FF4 doc lines 1032-1051 | App Editor / top bar | App Editor top bar with app selector, save status, live version, preview dropdown, publish, version history | The App Editor top bar must display: `[App: {name} ▼] Saved {time} Live: {live version} [Preview ▼] [Publish to Mobile] [Version History]`. The Preview dropdown must offer "Preview in Web Admin" and "Preview on Device...". | UI | must | Given the App Editor is open, then the top bar shows the app selector dropdown, autosave timestamp, current live version, preview dropdown, publish button, and version history button; Given the preview dropdown is clicked, then it shows two options: "Preview in Web Admin" and "Preview on Device...". | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-007 | FF4 doc lines 1053-1067 | App Editor / module reference selector | Every module reference in App Editor must select version resolution: Use newest or Use specific version | For each module in the app, the App Editor must show: module name, icon, enabled toggle, bottom bar slot assignment, and version source radio buttons (Use newest version or Use specific version with version selector dropdown). | UI | must | Given the App Editor shows a module reference, then it displays: module name, icon, enabled toggle, bottom bar slot, and version source radio buttons (Use newest / Use specific version with dropdown). | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-008 | FF4 doc lines 1070-1078 | App Editor / Use newest version | Use newest resolves to the newest valid Module Version at preview/publish time; published App Version stores exact version | When "Use newest" is selected, the App Editor working draft resolves to the newest valid Module Version. Published App Version stores the concrete resolved module version ID. A live published app must not silently change on mobile just because a newer module version appears later. | functional | must | Given "Use newest" is selected for a module, when the App Editor publishes, then the published App Version stores the concrete resolved module version ID; Given a live app is published with "Use newest", when a newer module version is created later, then the mobile app does not update until the App Editor publishes again. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-009 | FF4 doc lines 1080-1085 | App Editor / Use specific version | Pinned module version for stability; future module edits do not affect this app | When "Use specific version" is selected and a module version is pinned, the app stays on that exact Module Version. Future module edits do not affect this app unless the user changes the selection or switches to Use newest. | functional | must | Given "Use specific version" is selected and pinned to a Module Version, when that module's working draft is changed, then the app does not change; Given the pinned module version is changed by the user, then the app updates at next publish. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-010 | FF4 doc lines 1087-1114 | App Editor / version snapshot behavior | Published App Version must store both module reference policy and resolved module version IDs for reproducibility | When publishing, the App Version must store both the user's module reference policy (use_newest / specific_version) and the concrete resolved module version IDs. The published App Version must be reproducible later — restoring it must render with the exact module versions used at publish time. | data | must | Given an App Version is published, then the stored JSON includes for each module: policy (use_newest or specific_version) and resolvedModuleVersionId; Given an old App Version is restored, then it renders with the exact module versions from publish time, not whatever is newest. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-011 | FF4 doc lines 1240-1278 | App Editor / publish modal and flow | Publish confirmation modal showing app name, version name, assigned devices, module versions included, validation results, and publish action | The Publish to Mobile button must open a confirmation modal showing: app name, new version name (timestamp-based), assigned devices list, module versions included list, validation results with ✅/⚠️, and Cancel/Publish buttons. After publish, show device status (updated/pending). | UI | must | Given the user clicks "Publish to Mobile", then a modal appears showing app name, new version name, assigned devices, module versions, validation results; Given the user clicks "Publish", then the app version is created and device status is shown (updated/pending with last seen time). | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-012 | FF4 doc lines 1540-1554 | UI requirements / App Editor UI | Required UI elements for App Editor | The App Editor must include: app selector at top-left, iPhone-style app preview area, bottom bar editor, Launchpad editor, right inspector for app/module settings, module version selector per module (Use newest / Use specific), preview dropdown (Web Admin / Device), Publish to Mobile button, Version History button, device assignment UI, device update status after publish. | UI | must | Given the App Editor is open, then all listed UI elements are present and functional. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-013 | FF4 doc lines 9-10 | App Editor / module icons | Individual modules must have settings for their icons; cannot set Home/Chat icons currently | The App Editor must allow setting icons for individual modules (Home, Chat, etc.). The icon picker must be accessible from the module settings. | UI | must | Given the App Editor is editing a module's settings, when the user clicks the icon area, then an icon picker (emoji) opens; Given an icon is selected, then it displays in the module's icon position. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-014 | FF4 doc lines 1118-1187 | App Editor / full app web preview | Full app web preview inside the App Editor including app-level shell | "Preview in Web Admin" from App Editor must render the entire app including bottom bar, Launchpad, default launch module, theme, dark mode, and selected module versions. Must not create a mobile preview session. | functional | must | Given the App Editor clicks "Preview in Web Admin", then the full app renders in the web admin preview with bottom bar, Launchpad, theme, and dark mode; Given a module is disabled in the app, then it does not appear in the full app web preview. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-015 | FF4 doc lines 1155-1207 | App Editor / temporary mobile preview | Temporary mobile preview on real device from App Editor; creates Preview Session; mobile enters Preview Mode with clear indicator | "Preview on Device" from App Editor must: let user select a device, resolve app draft + module versions, validate against device runtime, create Preview Session, send preview_session_started event, device enters Preview Mode with visible indicator "Preview Mode — [App Name] — [Version]", and "Exit Preview" button. Exiting preview returns to current live version. | functional | must | Given the App Editor clicks "Preview on Device" and selects a device, then the device enters Preview Mode with a visible indicator; Given the user clicks "Exit Preview", then the device returns to its current live version; Given the preview session expires, then the device returns to live version. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-016 | FF4 doc lines 1199-1207 | App Editor / preview session rules | Preview sessions expire after timeout, do not change active_app_version_id, do not mark app as published | Preview sessions must: expire after configured timeout, allow resume only if still valid, exit returns to previous live version, must not change active_app_version_id, must not mark app as published, must log errors in admin. | functional | must | Given a preview session times out, then the device returns to live version; Given a preview session is active, then active_app_version_id is unchanged; Given a preview error occurs, then the error is logged in admin. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-017 | FF4 doc lines 1210-1238 | App Editor / publish flow | Publish creates immutable App Version, notifies devices, devices fetch and validate, atomically switch | Publishing must: run publish validation, create immutable App Version, mark as current_published_version_id, notify assigned devices via WebSocket, devices fetch new version, validate locally, cache, atomically switch. | workflow | must | Given the user publishes, then a new immutable App Version is created, an app_version_published event is sent via WebSocket, devices fetch and switch atomically; Given a device is offline during publish, then it updates on reconnection. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-018 | FF4 doc  lines 1281-1301 | App Editor / mobile live behavior | Mobile Live Mode: renders assigned_app.current_published_version; caches last known good version; offline renders cached version; updates atomically | In Live Mode, the mobile app must: on launch fetch assigned app and current published version, cache last known good App Version locally, if offline render cached version, if fetch fails keep cached version, receive app_version_published notification via WebSocket, apply updates atomically without half-updating. | functional | must | Given the mobile app launches, then it fetches assigned app and published version; Given the app is offline, then it renders the last known good cached version; Given an app_version_published event arrives, then the app fetches and applies the new version atomically. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-019 | FF4 doc lines 1315-1333 | App Editor / device update protocol | WebSocket notification only, REST fetch for actual data | When a new app version is published, the backend must send a WebSocket event `app_version_published` with appId, appVersionId, publishedAt. The mobile device must then fetch via REST: GET /devices/:deviceId/assigned-app and GET /apps/:appId/versions/:versionId. Do not send entire app JSON via WebSocket. | architecture | must | Given a publish occurs, then a WebSocket event app_version_published is sent; Given the device receives the event, then it fetches the full version via REST; Given the WebSocket event is lost, then the device still gets the correct version on next REST poll. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-020 | FF4 doc lines 27-28 | App Editor / app preview rework | App Preview must render the exact React Native app directly in web admin, not simulate it | The App Preview must be reworked to render closer to the actual React Native app experience inside the web admin, instead of creating a separate simulation. | UI | should | Given the App Editor preview is open, then the rendered preview closely matches the actual React Native mobile rendering. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-021 | FF4 doc lines 1581-1592 | App Editor / edge case — edit module used by live app | Editing module changes only working draft, not live app until App Editor publishes | When a user edits a module that is used by a live app, the edit changes only the module working draft. The live app does not change until the App Editor publishes an app version that resolves to that module version. | functional | must | Given a module is used by a live app, when the module working draft is edited, then the live app does not change; Given the App Editor publishes a new version that includes the edited module, then the live app updates. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-022 | FF4 doc lines 1593-1601 | App Editor / edge case — deleted module version | If a selected module version is deleted/archived, show warning in App Editor with guidance | If an app references an archived module version, the App Editor must show a warning: "This app references an archived module version. Choose a different version or restore the archived version." Must not break the app silently. | functional | must | Given an app references an archived module version, then the App Editor shows a visible warning with options to choose a different version or restore the archived version. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-023 | FF4 doc lines 1602-1604 | App Editor / edge case — device offline during publish | Publish succeeds on backend; device status becomes pending; updates on reconnect | When a device is offline during publish, the publish must still succeed on the backend. The device status must become "pending". When the device reconnects, it must fetch and apply the latest published App Version. | functional | must | Given a device is offline when publish occurs, then the backend publish succeeds; Given the device reconnects, then it fetches and applies the latest published App Version. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-024 | FF4 doc lines 1606-1615 | App Editor / edge case — device cannot render | Device keeps last known good version and reports error to backend; admin shows detailed error | If a device cannot render a published version (unsupported component type, incompatible runtime), the device must keep its last known good version and report the error to the backend. The admin must show a clear error: "Barry's iPhone failed to update: Unsupported component type 'ArticleCard'. Installed runtime: [version]. Required runtime: [version]." | functional | must | Given a device receives a version with an unsupported component type, then the device keeps its previous version and reports error; Given the backend receives the error, then the admin UI shows the error with device name, unsupported type, installed and required runtime versions. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-025 | FF4 doc lines 1617-1619 | App Editor / edge case — mobile preview fails | Device exits preview and returns to live version; backend logs failure | When mobile preview fails, the device must exit preview mode and return to the live version. The backend must log the preview failure. | functional | must | Given a mobile preview session fails, then the device returns to live mode; Given a preview failure occurs, then the backend logs the failure. | manual-flow-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-026 | FF4 doc lines 1621-1623 | App Editor / edge case — restore old version | Restore copies old version into working draft, does not auto-publish | When a user restores an old version, it must copy the old version into the current working draft. Restore must not automatically publish. The user must preview/publish again from the App Editor. | functional | must | Given an old App Version is restored, then the working draft contains the old version's config; Given a restore occurs, then the mobile app does not change until the user publishes again. | automated-test | FF4-SLICE-APP-EDITOR |
| FF4-APP-027 | FF4 doc lines 7, 3-4 | App Editor / cleanup test data | Testing must clean up after itself; no random Test App/New App junk | Test data (Test apps, New apps, test modules) must be removed or recovered to a functional state after testing. The main sample template must remain. The App Editor must provide cleanup capabilities for test data. | QA | must | Given test apps/modules exist, then a cleanup mechanism can remove them; Given cleanup is performed, then only the main sample/template remains. | automated-test | FF4-SLICE-APP-EDITOR |
| **Module Editor Requirements** |
| FF4-MOD-001 | FF4 doc lines 19-21 | Visual Editor / naming | Rename App Editor and Visual Editor to Module Editor and App Editor | The naming must be changed: "App Editor" stays as the app composition tool; "Visual Editor" must be renamed to "Module Editor" throughout the UI and code. | UI | must | Given the admin UI sidebar, then "Module Editor" and "App Editor" are the labels used (no "Visual Editor"). | review-only | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-002 | FF4 doc lines 21-22 | Visual Editor / sidebar structure | Collapsible sidebar structure works well; keep | The collapsible sidebar structure in the Module Editor must be maintained. | UI | should | Given the Module Editor is open, then the sidebar can be collapsed and expanded. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-003 | FF4 doc lines 23-24 | Visual Editor / top bar awkwardness | Top bar draft/approve/reject is awkward; addressed in versioning model | The Module Editor top bar must not use the "draft v1 / approve / reject" system. Instead use the document-style autosave/checkpoints/version history model. | UI | must | Given the Module Editor is open, then the top bar shows document-style controls (autosave status, checkpoint, preview, version history), NOT "Draft v1 | Approve | Reject". | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-004 | FF4 doc lines 25-26 | Visual Editor / save button greyed out | Save button greyed out when creating new module; cannot save as separate module | The Module Editor must allow creating a new module and saving it. The save button must not be greyed out when editing a new module. The user must be able to create and save a module as a separate entity. | functional | must | Given a new module is created in the Module Editor, when the user edits the module, then the save button is enabled; Given the user saves, then the module is persisted as a new module entity. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-005 | FF4 doc lines 27-28 | Visual Editor / app preview rework | App Preview must render exact React Native app directly in web admin instead of creating something new and simulating | The app preview in the Module Editor must be reworked closer to the actual React Native rendering, not a simulation. | UI | should | Given the Module Editor preview is open, then the rendered preview closely matches the actual React Native mobile rendering. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-006 | FF4 doc lines 29-30 | Visual Editor / right-click context menu | Right-clicking on a module pops up blank; cannot delete module from context menu | Right-clicking on a module must show a functional context menu allowing delete and other module operations. The context menu must not be blank. | UI | must | Given the user right-clicks on a module in the editor, then a context menu appears with delete and other actions; Given the user clicks delete, then the module is deleted. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-007 | FF4 doc lines 31-32 | Visual Editor / template component validity | Some templates use non-existent components; must fix | All templates must use only existing, valid component types. Templates using non-existent components must be fixed. | functional | must | Given any template is loaded or applied, then all component types referenced exist in the component registry. | automated-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-008 | FF4 doc lines 896-922 | Module Editor / responsibilities | Module Editor owns row/cell/component editing, props, bindings, actions, module name/icon, working draft, checkpoints, web preview. Does not own bottom bar, Launchpad, device assignment, full-app preview, mobile preview, publishing. | The Module Editor must own: row/cell/component canvas editing, component props, bindings, actions/rules, module-level name and icon, module working draft, module checkpoints/version history, web-admin module preview. The Module Editor must NOT own: bottom bar position, Launchpad membership, device assignment, full-app preview, mobile-device preview, publishing to mobile. | architecture | must | Given the Module Editor is open, then it provides all listed responsibilities; Given the Module Editor is open, then bottom bar, Launchpad, device assignment, full-app preview, mobile preview, and publish controls are not present. | review-only | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-009 | FF4 doc lines 922-935 | Module Editor / top bar | Module Editor top bar: [Module: {name} ▼] Saved {time} [Create Checkpoint] [Preview in Web Admin] [Version History] | The Module Editor top bar must display: `[Module: {name} ▼] Saved {time} [Create Checkpoint] [Preview in Web Admin] [Version History]`. Must NOT show Draft v1 / Approve / Reject / Push to Mobile. | UI | must | Given the Module Editor is open, then the top bar shows the module selector, autosave timestamp, Create Checkpoint button, Preview in Web Admin button, and Version History button; Given the top bar is visible, then it does NOT contain "Draft v1", "Approve", "Reject", or "Push to Mobile". | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-010 | FF4 doc lines 936-946 | Module Editor / autosave | Every edit autosaves into Module Working Draft; debounced; shows save state (Saving.../Saved {time}/Save failed); if fail keep local state with retry; no visible version for every keystroke | The Module Editor must autosave every edit into the Module Working Draft with debounce after edits. Must show save state: "Saving...", "Saved {time}", "Save failed". If save fails, keep local unsaved state and show retry. Must not create a new visible version for every keystroke/drag. May create internal recovery snapshots periodically without cluttering visible version tree. | functional | must | Given the user edits the module, then autosave is triggered after debounce; Given save succeeds, then the UI shows "Saved {time}"; Given save fails, then local state is preserved and "Save failed" with retry option is shown; Given rapid keystrokes, then only one visible version is created (not one per keystroke). | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-011 | FF4 doc lines 948-963 | Module Editor / checkpoints | User can create checkpoint from working draft; triggers include user click, before template apply, before restore, before risky bulk op, optional after major AI change; timestamp naming | Checkpoints must be creatable from the current working draft. Triggers: user clicks "Create Checkpoint", before applying a template over existing module, before restore, before risky bulk operation, optional automatic after major AI-generated change. Naming: default timestamp name, optional AI-generated suffix from change summary, user can rename. | workflow | must | Given the Module Editor is editing a draft, when the user clicks "Create Checkpoint", then a checkpoint is created with timestamp name; Given a template is about to be applied to an existing module, then a checkpoint is auto-created first; Given a checkpoint is created, then the user can rename it. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-012 | FF4 doc lines 965-977 | Module Editor / web preview | Module Editor preview is web-admin-only; renders current module draft; uses SDUI renderer close to mobile; shows viewport/device size; shows validation warnings inline; no mobile preview session | The Module Editor preview must: render only inside web admin, render the current module working draft, use the same SDUI renderer logic as close as possible to mobile, show selected viewport/device size if available, show validation warnings inline. Must NOT create a mobile preview session or overwrite any app live version. | functional | must | Given the Module Editor clicks "Preview in Web Admin", then the module draft renders in web admin; Given validation warnings exist, then they are shown inline in the preview; Given the preview is active, then no mobile preview session is created. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-013 | FF4 doc lines 979-983 | Module Editor / version creation | Module Version created when working draft is checkpointed/finalized; makes it available for App Editor references; does not update mobile | Creating a Module Version makes the module version available for App Editor references. Creating a Module Version does NOT update mobile by itself. Mobile only changes after App Editor preview/publish. | functional | must | Given a module checkpoint/version is created, then it appears in the App Editor's module version selector; Given a module version is created, then the mobile app does not change. | automated-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-014 | FF4 doc lines 985-1005 | Module Editor / restore | Restoring a Module Version copies its SDUI JSON into Module Working Draft; replaces current draft; autosaves; does not publish to mobile | When a user restores a Module Version, the system must: copy that version's SDUI JSON into the Module Working Draft, replace the current working draft, autosave, allow web admin preview. Restore must NOT publish to mobile. | workflow | must | Given an old module version is selected, when the user clicks "Restore to Working Draft", then the current working draft is replaced with the version's SDUI JSON; Given a restore occurs, then the mobile app does not change. | automated-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-015 | FF4 doc lines 1526-1538 | UI requirements / Module Editor UI | Required Module Editor UI elements | The Module Editor must include: collapsible module tree in sidebar, current module name/icon, autosave status, Create Checkpoint button, Preview in Web Admin button, Version History button. Must NOT include: mobile preview button, Publish to Mobile button, Approve/Reject terminology. | UI | must | Given the Module Editor is open, then all listed UI elements are present and functional; Given the Module Editor is open, then mobile preview, publish, and approve/reject controls are absent. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| FF4-MOD-016 | FF4 doc lines 29-30, 656-662 | Module Editor / row context menu | Right-click on row shows context menu: Add Row Above, Add Row Below, Duplicate Row, Delete Row | The Module Editor must show a `RowContextMenu` component when right-clicking on any row in the editor canvas. Menu options: Add Row Above, Add Row Below, Duplicate Row, Delete Row (with confirmation if cells contain content). | UI | must | Given the user right-clicks on a row, then the context menu appears with Add Above, Add Below, Duplicate, Delete; Given the user clicks Delete Row on a row with content, then a confirmation dialog appears; Given the user clicks Duplicate Row, then a new row is created with identical cells and components. | manual-flow-test | FF4-SLICE-MODULE-EDITOR |
| **Row Requirements** |
| FF4-ROW-001 | FF4 doc lines 35-36 | Visual Editor / Rows / drag handler | Drag handler (6 dots) must be visible on the left side of the row, not within the row but to the left, not within the screen; supports smooth dragging | The row drag handler (6 dots) must be visible on the LEFT side of the row, OUTSIDE the row boundary (not within the row, not within the screen). It must support smooth dragging actions without lag to change row order. | UI | must | Given the Module Editor canvas, then every row has a visible 6-dot drag handle to its left, outside the row boundary; Given the user drags a row by its handle, then the row moves smoothly without lag. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-002 | FF4 doc lines 37-38 | Visual Editor / Rows / resize lag | Row resize size drags behind mouse; not fully following | Row height resize must follow the cursor smoothly without lagging behind. The visual feedback must be immediate. | UI | must | Given the user drags a row height resize handle, then the row boundary follows the cursor in real-time without perceptible lag. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-003 | FF4 doc lines 39-50 | Visual Editor / Rows / cell width calculation | Cells calculated as percentages of row width; default "auto"; formula: fixedTotal + remaining / autoCellCount | Cell widths must be calculated as percentages of the row width. The default width for a new cell must be "auto". Formula: `fixedTotal = sum(width % of all manually-sized cells)`, `remaining = 100% - fixedTotal`, `autoCellWidth = remaining / numberOfAutoCells`. Example: Cell1=50%, Cell2=auto, Cell3=auto → remaining=50%, autoCellWidth=25% → Final row = 50%/25%/25%. | functional | must | Given a row with Cell1=50%, Cell2=auto, Cell3=auto, then resolved widths are 50%, 25%, 25%; Given a new cell is added, then its default width is "auto". | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-004 | FF4 doc lines 66-83 | Visual Editor / Rows / minimum width rule | Every cell must have minimum width based on usable row content width, not raw row width; formula: usableRowWidth = rowWidth - horizontalPadding - gaps; minWidthPercent = minCellWidthPx / usableRowWidth * 100 | The minimum cell width must be calculated from usable row content width, not raw row width. Formula: `usableRowWidth = rowWidth - horizontalPadding - gaps`, `minWidthPercent = minCellWidthPx / usableRowWidthPx * 100`. Validation: `resolvedCellWidthPercent >= minWidthPercent`. If the user increases padding so much that cells cannot fit minimum width, block the padding change. Minimum cell width: 80px. | functional | must | Given a row with specific padding and gap values, then minWidthPercent is calculated from usableRowWidth; Given a resolved cell width is below minWidthPercent, then the configuration is invalid; Given the user attempts to increase padding that would violate minimum width, then the action is blocked. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-005 | FF4 doc lines 85-88 | Visual Editor / Rows / action blocking | Block any user action before it commits if it would make the row invalid; do not apply change first then let row break visually | The editor must block any user action BEFORE it commits if that action would make the row invalid. Must NOT apply the change first and then let the row break visually. | functional | must | Given an action would make the row invalid, when the user attempts it, then the action is blocked before any visual change occurs; Given an invalid action is blocked, then the row state remains unchanged. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-006 | FF4 doc lines 89-101 | Visual Editor / Rows / pre-flight next-action check | Continuously check whether next possible action is valid before user clicks; disable/grey-out controls if they would break rules; show explanation why | The editor must continuously check whether the next possible action would be valid. If the next action would break the row rules, that control must be disabled/greyed out with an explanation. Examples: Add Cell greyed out if minimum width violated; width increase blocked if fixedTotal > 100%; width change blocked if auto cells would shrink below minimum; padding increase greyed out if cells can't fit; resize handle stops at minimum valid point; horizontal scroll toggle disabled/confirmation if overflow. | UI | must | Given adding a cell would make any cell smaller than minimum width, then the Add Cell button is greyed out; Given increasing a fixed cell width would make fixedTotal > 100%, then the width increase is blocked; Given increasing padding would reduce usable row width below minimum, then the increase-padding control is greyed out; For each disabled control, an explanation is shown (e.g., "Cannot add another cell: minimum cell width would be violated"). | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-007 | FF4 doc lines 137-141 | Visual Editor / Rows / horizontal scrolling rule | With scrolling off, must not silently turn on; must not allow cells to overflow; block actions that would require scrolling; with scrolling on, allow more cells but minimum width still applies | If horizontal scrolling is turned off, the row must NOT silently turn scrolling on. The editor must NOT allow cells to overflow outside the row. If a change would require scrolling/overflow and scrolling is disabled, block the action and show warning. If horizontal scrolling is on, row can allow more cells but minimum width still applies (scrolling is not a way to shrink cells below minimum). | functional | must | Given horizontal scrolling is off, when an action would cause overflow, then the action is blocked with a warning; Given horizontal scrolling is on, then minimum width rule still applies even with scrolling. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-008 | FF4 doc lines 143-161 | Visual Editor / Rows / all fixed-width cells rule | If all cells fixed and total < 100%, cells center with side padding, do not stretch | If all cells have fixed widths and their total is less than 100%, the cells must NOT stretch to fill remaining space. Instead, the row must center the cells and treat leftover space as equal side padding. Example: Cell1=30%, Cell2=30%, fixedTotal=60%, leftover=40% → 20% padding left, 30% cell, 30% cell, 20% padding right. | UI | must | Given a row has Cell1=30%, Cell2=30% (all fixed, total=60%), then the cells are centered with 20% padding left and 20% padding right. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-009 | FF4 doc lines 163-165 | Visual Editor / Rows / mixed fixed+auto rule | If at least one cell is auto, leftover goes to auto cells, not side padding | If at least one cell is "auto", then all leftover width after fixed cells must go to the auto cells. Leftover width must NOT become side padding when any auto cell exists. Side padding only applies when ALL cells are fixed-width and fixedTotal < 100%. | functional | must | Given a row with Cell1=50%, Cell2=auto (mixed), then leftover 50% goes to Cell2, not to side padding. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-010 | FF4 doc lines 167-184 | Visual Editor / Rows / validation timing | Validation runs for all relevant operations: add cell, delete cell, change auto↔fixed, type width, drag divider, resize row, change padding/gaps, toggle scrolling, load saved row, import template | Validation must run for every operation: adding cell, deleting cell, changing auto↔fixed, typing a width in config panel, dragging a cell divider, resizing the row, changing row padding, changing row gaps, toggling horizontal scrolling, loading an existing saved row, importing/applying a template. | functional | must | Given any of the listed operations occur, then validation runs and blocks invalid states. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-011 | FF4 doc lines 184 | Visual Editor / Rows / saved row validation error | If existing saved row or template violates rules, show validation error and refuse to publish; do not silently normalize | If an existing saved row or template violates the row rules, the editor must show a validation error and refuse to publish until fixed. Must NOT silently normalize the invalid layout into a different layout. | validation | must | Given a saved row has invalid cell widths, when the editor loads it, then a validation error is shown; Given an invalid loaded row, when user attempts to publish, then publish is refused with an error. | automated-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-012 | FF4 doc lines 186-187 | Visual Editor / Rows / fit everything | Cells resize to fit rows (stretches); but atomic components inside cells do not fit; components MUST fill entire cell | Every component inside a cell must fill the entire cell dimensions. If a cell is 80px × 80px, a button inside it must fill that entire space. Buttons do not need to be resized — they must fill their cell automatically. Same for input bars and all other components. | UI | must | Given a cell is 80px×80px, then any component inside fills the entire 80px×80px space; Given a cell resizes, then the component inside resizes to fill the new cell dimensions. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-013 | FF4 doc lines 190-191 | Visual Editor / Rows / remove space features | Remove all space-related features for rows: padding, gap | All space-related features for rows (padding, gap) must be removed. Rows are clean containers with no spacing properties. | UI | must | Given a row is selected in the editor, then the property inspector does not show padding or gap controls; Given the SDUI JSON for a row, then padding and gap properties are absent. | review-only | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-014 | FF4 doc lines 190-191 | Visual Editor / Rows / remove color features | Remove color-related settings in rows: no background colors | Color-related settings in rows (background colors) must be removed. | UI | must | Given a row is selected in the editor, then the property inspector does not show background color controls; Given the SDUI JSON for a row, then backgroundColor property is absent. | review-only | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-ROW-015 | FF4 doc lines 192-193 | Visual Editor / Rows / minimum enforcement as drag limit | Instead of letting drag go below minimum then bouncing back, prevent drag from going below minimum at all | For minimum widths and heights enforcement: instead of letting the user drag below the minimum and having it bounce back, prevent the drag handle from moving below the minimum entirely. The drag must stop at the minimum boundary. | UI | must | Given the user drags a row height handle toward the minimum (48px), then the handle stops at 48px and cannot go lower; Given a cell width drag approaches minimum, then the handle stops at the minimum percentage and cannot go lower. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| **Cell Requirements** |
| FF4-CELL-001 | FF4 doc lines 194-196 | Visual Editor / Cells / resizing behavior | Cell resize must follow cursor; must not move other cells when resizing one cell; must follow minimum width rule | Cell width resizing must follow the cursor precisely. Resizing one cell must not cause other cells to move in unexpected ways. Cell resizing must follow the minimum width rule. | UI | must | Given the user drags a cell divider to resize a cell, then the divider follows the cursor; Given a cell is resized, then adjacent cells do not jump or move unexpectedly; Given a resize would violate minimum width, then the resize is blocked at the minimum boundary. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-CELL-002 | FF4 doc lines 198-199 | Visual Editor / Cells / delete button overlap | Row's delete "x" overlaps with rightmost cell's "x"; move row's "x" to the left, cells' "x" on the left | The row's delete button ("x") overlaps with the rightmost cell's delete button ("x"). The row's "x" must be moved to the left side. Cell delete buttons must be on the left side of the cell. | UI | must | Given a row with cells is displayed, then the row's delete button is on the left side of the row; Given a cell is displayed, then the cell's delete button is on the left side of the cell and does not overlap with the row's delete button. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-CELL-003 | FF4 doc lines 39, 186-187 | Visual Editor / Cells / more than 6 cells allowed | Can add more than 6 cells now; minimum width rules must still be correctly enforced | The editor must allow adding more than 6 cells per row. However, minimum width rules must still be correctly enforced for all cells. | functional | must | Given a row with sufficient width, when the user adds a 7th cell, then it is allowed; Given a row where adding a 7th cell would violate minimum width, then the Add Cell button is greyed out. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| FF4-CELL-004 | FF4 doc lines 258-259, 291-298 | Visual Editor / Components / fit-the-cell | All components must fit their cell; Calendar, buttons, inputs, etc. must flex to fill cell dimensions | All components must fit their cell. If the cell resizes, the component must resize. Components that cannot fit (e.g., Calendar variant too large for cell) must show validation warning. Components should use flex: 1, width: '100%', height: '100%' inside cells. | UI | must | Given any component is placed in a cell, then it fills the entire cell area; Given a Calendar variant cannot fit the cell, then a validation warning is shown. | manual-flow-test | FF4-SLICE-ROWS-CELLS-LAYOUT |
| **Variable System Requirements** |
| FF4-VAR-001 | FF4 doc lines 200-202 | Visual Editor / Variable system | Variable system is complicated across many systems; pill UI with @ works well for insertion; hit box of pill is too big causing accidental clicks; variables don't display or preview | The variable system must work end-to-end: display correctly in preview, resolve variable values, show variables in rendered output. The pill UI with @ trigger for variable insertion must work well. The hit box (touch/click target) of variable pills must not be oversized — users must not accidentally click on pills when not actually selecting them. | functional | must | Given a variable is placed in text, when the preview renders, then the variable value is displayed; Given a variable pill is rendered, then its click target is proportional to its visual size — not oversized; Given the @ trigger is used, then the variable picker appears. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-VAR-002 | FF4 doc lines 202-203 | Visual Editor / Variable system / QA | Variable system must be tested with a better QA script | The variable system must have comprehensive QA coverage. A better QA script must be created to validate end-to-end variable resolution. | QA | must | Given a QA test suite for variables, then it covers variable insertion, display, rendering, and edge cases. | automated-test | FF4-SLICE-COMPONENTS |
| FF4-VAR-003 | FF4 doc lines 570-572 | Variables / sample variables | Need sample variables that can be tested to verify everything works | The system must include sample variables (e.g., user.name, app.theme, greeting.morning) that users and AI can test to verify variable resolution works end-to-end. | data | should | Given sample variables exist, when they are placed in a module, then they resolve to their configured values. | automated-test | FF4-SLICE-COMPONENTS |
| **Text/Markdown Requirements** |
| FF4-TEXT-001 | FF4 doc lines 204-208, 224-226 | Visual Editor / Text + Text input | Remove current Text display component; remove TextInput form component (replaced by InputBar); rename Markdown as "Text" | The current `Text` display component must be removed. The `TextInput` form component must be removed entirely (InputBar replaces it per source lines 224-226). The `Markdown` component must be renamed to `Text`. After the merge, "Text" is markdown-based using react-native-markdown-display. The old `SDUIMarkdown` must be removed. | functional | must | Given the component registry, then `Text` is the only text component (no separate `Markdown`, no `TextInput`); Given the legacy `Markdown` type is used, then it is mapped to `Text` for backward compatibility; Given the component registry, then `TextInput` does not exist as a component. | review-only | FF4-SLICE-COMPONENTS |
| FF4-TEXT-002 | FF4 doc lines 210-214 | Visual Editor / Markdowns | Markdown enter key bug: hitting enter in config panel shows text on same line in preview instead of new line; must fix newline behavior | When typing in the Markdown config panel (property inspector), hitting the Enter key must produce a new line in the preview. Currently, Enter does not create a new line — all text continues on the same line. | functional | must | Given the user types text in the Markdown property inspector, when they press Enter, then the preview shows a new line break; Given multi-line markdown content, then all line breaks render correctly. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-TEXT-003 | FF4 doc lines 214-215 | Visual Editor / Text / alignment | Add text alignment feature: left, center, right align | Since Text and Markdown are merged, add one feature: text alignment (left, center, right). Everything else uses native markdown. | UI | must | Given the Text component property inspector, then an alignment control with left/center/right options is available; Given alignment is set to "center", then the rendered markdown text is center-aligned. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-TEXT-004 | FF4 doc lines 214-215 | Visual Editor / Text / template migration | All templates must be migrated to use the new combined Text (markdown-based) component | All existing templates that used the old Text or Markdown components must be migrated to use the new combined Text component. | functional | must | Given any template is loaded, then all text/markdown component references use the new combined `Text` type; Given old `Markdown` type appears in a template, then it is migrated to `Text`. | automated-test | FF4-SLICE-COMPONENTS |
| FF4-TEXT-005 | FF4 doc lines 214 | Visual Editor / Text / variable compatibility | Variable system must work with the new combined Text (markdown-based) component | The variable system must work correctly within markdown content rendered by the new Text component. Variable references embedded in markdown must resolve and display. | functional | must | Given the Text component contains a variable reference like @username, when rendered, then the variable value is displayed within the markdown output. | automated-test | FF4-SLICE-COMPONENTS |
| **Button Requirements** |
| FF4-BTN-001 | FF4 doc lines 216-218 | Visual Editor / Buttons | Buttons must fit the entire cell under all circumstances; no size difference | Buttons must fill the entire cell under all circumstances. There must not be any size difference for buttons — they always fill their parent cell. | UI | must | Given a button is placed in a cell of any size, then the button fills the entire cell area. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-BTN-002 | FF4 doc lines 218-219 | Visual Editor / Buttons / icon mode | Icon mode for buttons is completely broken; nothing shows up on the icon part | The icon mode for buttons must work. When a button is configured with an icon, the icon must render correctly (not blank). | UI | must | Given a button is configured with icon mode and an icon, when rendered, then the icon is visible; Given icon mode is selected, then the icon renders centered within the button. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-BTN-003 | FF4 doc lines 219-220 | Visual Editor / Buttons / template-first architecture | Templates needed to test button actions; reference previous feature feedback for template-first approach | A template-first architecture must be used where functional templates demonstrate button actions end-to-end. Templates allow testing button actions without manual setup. | workflow | should | Given functional templates exist, then they contain buttons with configured actions that can be tested end-to-end. | manual-flow-test | FF4-SLICE-COMPONENTS |
| **Image Requirements** |
| FF4-IMG-001 | FF4 doc lines 220-222 | Visual Editor / Image | Image does not need to fit cell; has preference of width fitting or height fitting (fitWidth / fitHeight) | Images do not need to fill the entire cell like other components. Instead, images have a fit mode preference: fit the width (fitWidth) or fit the height (fitHeight). | functional | must | Given an image is placed in a cell, when fitMode is "fitWidth", then the image scales to fit the cell width while preserving aspect ratio; Given fitMode is "fitHeight", then the image scales to fit the cell height while preserving aspect ratio. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-IMG-002 | FF4 doc lines 222-223 | Visual Editor / Image / remove properties | Remove properties: alt text, width, height, aspect ratio (use original), border radius, resize mode → fit mode, action (per source "remove all these properties... action") | Image component must be simplified to only: `src` and `fitMode` (fitWidth/fitHeight). `action` must be removed (source says remove action). Remove: alt text, width, height, aspectRatio (image's original aspect ratio is used), borderRadius, onPress, placeholder, action. | functional | must | Given an image component's property inspector, then only src and fitMode properties are available; Given the SDUI JSON for an image, then removed properties (alt, width, height, aspectRatio, borderRadius, onPress, placeholder, action) are absent. | review-only | FF4-SLICE-COMPONENTS |
| **Icon Requirements** |
| FF4-ICON-001 | FF4 doc lines 228-230 | Visual Editor / Icons | Icons are completely broken; cannot change icons at all; need emoji picker popup for humans; icons must fit cell instead of corner | Icon changing must work. The icon picker must provide an emoji popup for humans. Icons must fill the entire cell (centered) instead of being positioned in the corner. | UI | must | Given the user clicks an icon in the editor, then an emoji picker popup opens; Given an icon is selected, then it renders centered in its cell, filling the cell area. | manual-flow-test | FF4-SLICE-COMPONENTS |
| **Empty Container Requirements** |
| FF4-EC-001 | FF4 doc lines 232-248 | Visual Editor / Empty container | Empty Container is just a vertical row; inherit Row system behavior rotated vertically: cells become vertical sub-cells, add/delete/reorder becomes vertical, minimum-size rules become vertical, drag becomes vertical, component-fitting rules still apply | The Empty Container must be implemented as a vertical row. Everything must be inherited from the existing Row system but rotated vertically: Row cells → vertical sub-cells, Row add/delete/reorder → vertical add/delete/reorder, Row minimum-size/validation rules → vertical minimum-height/validation rules, Row drag → vertical drag, Row component-fitting rules still apply. | architecture | must | Given an Empty Container is in a cell, then it behaves as a vertical row with cells stacked vertically; Given a component is added to an Empty Container, then it fills the sub-cell area; Given the Empty Container is resized, then the same validation rules as horizontal rows apply vertically. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-EC-002 | FF4 doc lines 245-246 | Visual Editor / Empty container / no separate system | Do not build a separate complicated layout system; do not expose fake/internal Container components; Empty Container must be a real editable component | The Empty Container must NOT be built as a separate complicated layout system. Must not expose fake/internal `Container` components. Empty Container must be a real editable component — not a hidden wrapper. | architecture | must | Given the Empty Container is placed in a cell, then it is a real, editable component in the component tree (not a hidden wrapper). | review-only | FF4-SLICE-COMPONENTS |
| FF4-EC-003 | FF4 doc lines 246-248 | Visual Editor / Empty container / remove styling | Remove extra styling/settings: no gap, no padding, no background | The Empty Container must be simplified by removing: gap, padding, and background color settings. | UI | must | Given an Empty Container is selected in the editor, then the property inspector does not show gap, padding, or background controls. | review-only | FF4-SLICE-COMPONENTS |
| FF4-EC-004 | FF4 doc lines 248-249 | Visual Editor / Empty container / Daily Planner test case | Daily Planner test case: one normal row → one cell → one Empty Container → Calendar / Todo / Notes stacked vertically | The Empty Container must support the Daily Planner test case: one normal row → one cell → one Empty Container (vertical row) → Calendar / Todo / Notes stacked vertically inside. | functional | should | Given the Daily Planner template uses an Empty Container, then Calendar, Todo, and Notes are stacked vertically and function correctly. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-EC-005 | FF4 doc lines 679-682 | SDUI Empty Container Props | Empty (SDUIEmpty) must support standard SDUI V2 props: dispatch for SDUIAction, dataBinding for data source integration | The `SDUIEmpty` component must support: `dispatch` — function to dispatch `SDUIAction` for interactive behavior, and `dataBinding` — `SDUIDataBinding` for data source integration. It must get real registry-component treatment without redesigning the vertical flex layout. | functional | must | Given an Empty container has a dispatch action configured, then it dispatches actions; Given an Empty container has a dataBinding configured, then it integrates with the data source. | automated-test | FF4-SLICE-COMPONENTS |
| **Calendar Requirements** |
| FF4-CAL-001 | FF4 doc lines 250-258 | Visual Editor / Calendar / real component | Calendar must be a real functional first-class component, not fake/static template element; one first-class Component with admin-controlled variant setting | The Calendar must be one first-class Component in the registry with an admin-controlled `variant` setting. Must not be a fake/static template element. Must not be a bunch of separate fake calendar components. The admin/module editor chooses which variant appears in a module; the frontend renders that exact variant. | architecture | must | Given the Calendar is in the component registry, then it is a real component with multiple variants; Given the admin selects a variant, then that exact variant renders on mobile. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-002 | FF4 doc lines 260-266 | Visual Editor / Calendar / variants | Supported variants: Month (full month grid), Week (time-block), Day (time-block), Event List (vertical upcoming events), Compact (small dashboard widget) | The Calendar must support 5 variants: Month (full month grid, 7 columns), Week (week time-block view), Day (day time-block view), Event List (no grid, vertical upcoming events list), Compact (small dashboard/widget version for small cells). | functional | must | Given the admin selects "Month" variant, then a full month grid is rendered; Given the admin selects "Compact", then a small dashboard widget renders; All 5 variants must render real layouts, not fake placeholders. | automated-test | FF4-SLICE-CALENDAR |
| FF4-CAL-003 | FF4 doc lines 261-266 | Visual Editor / Calendar / event list variant | Event List: vertical list of upcoming events, shows title/time/date/source color/metadata; supports filters | The Event List variant must show: vertical list of upcoming events, each row with event title, time, date if not today, source color, optional metadata. Must support filters (only certain event types/categories). | UI | must | Given the Event List variant is active, then upcoming events display in a vertical list with title, time, source color; Given filters are configured, then only matching events display. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-004 | FF4 doc lines 351-366 | Visual Editor / Calendar / compact variant | Compact variant fits 50/50 dashboard row; shows event count and next event; useful at a glance; used in Personal Dashboard | The Compact variant must fit in a 50/50 dashboard row. It must show: event count ("3 events today"), next event ("Next: 2:00 PM — Team sync"). Must not render a full month grid. Must be useful at a glance. This is the variant used in the Personal Dashboard template next to Weather. | UI | must | Given the Compact variant is in a 50/50 cell, then it renders event count and next event summary; Given the cell is small, then no month grid is rendered. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-005 | FF4 doc lines 268-273 | Visual Editor / Calendar / variant control rule | Variant switching is admin-controlled only; mobile user must not see Month/Week/Day/List/Compact switcher buttons; mobile user can still navigate time | The mobile user must NOT see Month/Week/Day/List/Compact switcher buttons. Variant switching is admin-controlled only. The mobile user can still navigate time within the chosen variant (e.g., next week, previous week, today) but cannot switch the component into another variant. | UI | must | Given the Calendar renders on mobile, then no variant switcher UI is visible; Given the admin has set the variant to "Week", then the mobile user can navigate to next/previous week but cannot switch to Month view. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-006 | FF4 doc lines 275-289 | Visual Editor / Calendar / date navigation | Every date-based variant must have built-in navigation header: ◀ [Current range label] ▶ [Today]; built into Calendar, not manual buttons | Every date-based variant must keep a small built-in navigation header: `◀ [Current range label] ▶ [Today]`. Examples: Month: `◀ May 2026 ▶ [Today]`, Week: `◀ May 11–17 ▶ [Today]`, Day: `◀ Wed May 13 ▶ [Today]`. This date navigation must be part of the Calendar Component itself, not something built manually. | UI | must | Given a Month variant Calendar renders, then `◀ May 2026 ▶ [Today]` is visible; Given the user clicks ▶, then the view advances to the next period; Given the user clicks Today, then the view returns to today's date. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-007 | FF4 doc lines 291-298 | Visual Editor / Calendar / fit-the-cell | Calendar must respect current cell size; auto-adapt variant if selected one cannot fit; show validation warning if variant cannot fit | The Calendar must respect the current cell size. If the Calendar is in a small cell, it should use Compact or Event List. Month/Week/Day can require more space but must not overflow the row/cell. The component must not silently break layout or force cells outside boundaries. If the selected variant cannot fit the current cell, show validation warning/error before publish. | functional | must | Given a Calendar is in a small cell (width < 200px), then it auto-adapts to Compact or Event List; Given a selected variant cannot fit the cell, then a validation warning is shown; Given the Calendar renders, then it does not overflow or break the layout. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-008 | FF4 doc lines 301-312 | Visual Editor / Calendar / month variant | Month view: 7-column grid, today highlighted, clickable dates, days with events show colored dots/indicators, color indicates event source; phone: tap date → agenda list; tablet/wide: event pills inside day cells; tap event → details | Month variant behavior: 7-column month grid, today highlighted, each date clickable/tappable, days with events show colored dots/small indicators, color indicates event source/calendar. Phone behavior: tapping a date populates lower part with that day's agenda list. Tablet/wide-screen: show event pills directly inside day cells when enough space (Notion Calendar style). Tapping an event opens event details. | UI | must | Given the Month variant on phone, when a date with events is tapped, then the day's agenda list appears below; Given an event pill is tapped, then event details open. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-009 | FF4 doc lines 314-323 | Visual Editor / Calendar / week/day variants | Week/Day: time-block grid, vertical time axis 00:00–23:59, events as positioned blocks, overlapping handled cleanly, current time indicator if easy, swipe/arrows to move dates, tap event → details | Week and Day variants must use a time-block grid: vertical axis = time from 00:00 to 23:59, events appear as positioned blocks based on start/end time, overlapping events handled cleanly (side-by-side or stacked), current time indicator supported if easy, user can swipe or use arrows to move through dates, tapping an event opens details. | UI | must | Given Week variant, then events are positioned blocks at their correct time slots; Given overlapping events, then they are displayed side-by-side or stacked cleanly; Given an event block is tapped, then event details open. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-010 | FF4 doc lines 325-340 | Visual Editor / Calendar / 3-day view | 3-day time-block view: X = 3 dates, Y = 00:00–23:59, colored event blocks, swipe to move 3-day window, tap → details; implement as separate variant or configurable mode later | The 3-day view idea should be kept in mind: X-axis = 3 dates, Y-axis = 00:00–23:59, events as colored blocks in correct time slots, swipe to move 3-day window, tapping event opens details. Can be implemented as separate `ThreeDay` variant later or configurable Week/Day mode. Do not overcomplicate V1 but keep the idea. | functional | deferred | Deferred for future implementation. | deferred | FF4-SLICE-CALENDAR |
| FF4-CAL-011 | FF4 doc lines 367-393 | Visual Editor / Calendar / event detail | Tapping an event shows detail: title, start/end time, all-day, source name/color, location, description/notes, reminder info; Notion events show richer property card | When the user taps an event, show a detail surface. Use full-screen navigation or inline detail panel (no modals/bottom sheets). Normal events show: title, start/end time, all-day status, calendar/source name, source color, location (if available), description/notes (if available), reminder info. Notion-backed events show richer property card: title, date/time range, reminder, status, tags, custom properties, notes/content, link to original Notion page. | UI | must | Given an event is tapped, then the event detail surface opens; Given a normal event, then title, time, source, and notes are displayed; Given a Notion-backed event, then richer properties are displayed. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-012 | FF4 doc lines 395-416 | Visual Editor / Calendar / unified event model | Frontend receives unified event array; event shape: id, title, start, end, allDay, sourceId, sourceName, sourceType (local/caldav/notion/custom), sourceColor, properties (flexible object) | The frontend must receive a unified event array from the backend. Event shape: `id`, `title`, `start`, `end`, `allDay`, `sourceId`, `sourceName`, `sourceType` (local/caldav/notion/custom), `sourceColor`, `properties` (flexible object for Notion page/database properties). The frontend must not care where events come from. | data | must | Given events are fetched, then all events use the unified event shape; Given a Notion-backed event, then properties object includes Notion-specific fields without breaking the schema. | automated-test | FF4-SLICE-CALENDAR |
| FF4-CAL-013 | FF4 doc lines 418-425 | Visual Editor / Calendar / local-first data | Local SQLite event table works first; remote connections sync into local representation; Calendar UI reads from local/unified model; connector/sync layer handles remote sync | Calendar must follow local-first architecture: Local SQLite event table works first, remote connections are optional and sync into local representation, Calendar UI reads from local/unified event model, remote source sync is handled by a connector/sync layer, not by Calendar component directly. | architecture | must | Given the Calendar component renders, then it reads from the local/unified event model; Given a remote source is connected, then events sync into the local model via the connector/sync layer. | automated-test | FF4-SLICE-CALENDAR |
| FF4-CAL-014 | FF4 doc lines 427-432 | Visual Editor / Calendar / sources | Sources to support: Local events first, CalDAV later (Google/iCloud/Fastmail/Nextcloud), Notion database later, Custom API later | The Calendar must support event sources in priority order: Local events first, CalDAV later (Google Calendar, iCloud, Fastmail, Nextcloud), Notion database/page date properties later, Custom API sources later. | functional | must | Given the Calendar renders, then local events are displayed; Given CalDAV is supported, then CalDAV events appear via the unified event model. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-015 | FF4 doc lines 434-438 | Visual Editor / Calendar / color rule | Each source/calendar gets auto-assigned color for MVP; user-customizable colors deferred; color must stay consistent across Month dots, Week/Day blocks, Event List rows, event detail headers | Each source/calendar must get an auto-assigned color for MVP. User-customizable colors are deferred QoL. Color must stay consistent across all displays: Month dots, Week/Day blocks, Event List rows, event detail headers. | UI | must | Given a source has an assigned color, then the same color appears in Month dots, Week/Day blocks, Event List rows, and event detail headers. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-016 | FF4 doc lines 440-449 | Visual Editor / Calendar / filtering | Admin can configure: all events, only selected sources/calendars, only selected event categories/types, possibly events matching certain properties later | The admin must be able to configure Calendar filtering: all events, only selected sources/calendars, only selected event categories/types, possibly events matching certain properties later. Important for templates: compact dashboard calendar might show only today's school/project events while full Calendar shows everything. | functional | must | Given admin configures filters, then only matching events display; Given a compact dashboard Calendar, then it can be filtered to show only specific event types. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-017 | FF4 doc lines 451-464 | Visual Editor / Calendar / data binding | Calendar must be tested with real data binding, not static/mock data; minimum tests: renders events from local data, Compact shows correct count/next event, Event List correct order, Month indicators on correct dates, Week/Day positions correct by time, date navigation changes range, event detail opens correctly, source colors consistent | The Calendar must be tested with real data binding (not static/mock data). Minimum tests: Calendar renders events from local data, Compact variant shows correct event count and next event, Event List shows upcoming events in correct order, Month view shows indicators on correct dates, Week/Day view positions events correctly by time, date navigation changes visible range, event detail opens and displays correct fields, source colors are consistent. | QA | must | Given real event data exists, then all Calendar variants display correct data; Given date navigation is used, then the visible range updates correctly. | automated-test | FF4-SLICE-CALENDAR |
| FF4-CAL-018 | FF4 doc lines 466-473 | Visual Editor / Calendar / libraries | Use react-native-calendars for Month grid, react-native-big-calendar for Day/Week time-block; wrap behind Helm's stable interface | The Calendar should use `react-native-calendars` for Month grid (supports dot marking, day press behavior) and `react-native-big-calendar` for Day/Week/3-day time-block layouts. Must wrap these libraries behind Helm's own stable interface so SDUI schema does not change if underlying library changes. | architecture | should | Given the underlying calendar library changes, then the SDUI schema remains unchanged. | review-only | FF4-SLICE-CALENDAR |
| FF4-CAL-019 | FF4 doc lines 475-487 | Visual Editor / Calendar / inspector fields | Calendar inspector: variant selector (Month/Week/Day/Event List/Compact), title/display name, selected sources, filters/categories, default visible date behavior (today/current range), max events for Compact/Event List, show/hide event metadata options | The Calendar inspector/property panel must include: variant selector (Month/Week/Day/Event List/Compact), title/display name, selected sources/calendars, filters/categories, default visible date behavior (today/current range), max events for Compact/Event List, show/hide event metadata options. Must not expose low-value styling controls before functional behavior works. | UI | must | Given the Calendar component is selected in the inspector, then all listed property fields are available; Given non-functional styling controls, then they are hidden until functional behavior works. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-020 | FF4 doc lines 489-504 | Visual Editor / Calendar / template usage | Personal Dashboard: Weather + Compact Calendar in 50/50 row; Daily Planner: Calendar in Week variant inside Empty Container; Full Calendar module: Month or Week variant | Calendar must be used in templates: Personal Dashboard: Weather + Compact Calendar in 50/50 row, Daily Planner: Calendar in Week variant inside vertical Empty Container, Full Calendar module: Month or Week variant depending on template. | workflow | should | Given the Personal Dashboard template, then a 50/50 row with Weather and Compact Calendar renders; Given the Daily Planner template, then Calendar in Week variant appears inside the Empty Container. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-021 | FF4 doc lines 495-504 | Visual Editor / Calendar / acceptance criteria | Calendar is first-class real component; variants render real layouts; Compact works in small cells; Month/Week/Day/Event List don't overflow; user navigates dates but can't switch variants; admin can select variant and filters; event details open correctly; local event data renders end-to-end; future sources map to unified model | Acceptance criteria: Calendar is first-class real component in registry, variants render real layouts (not fake placeholders), Compact works inside small cells, Month/Week/Day/Event List do not overflow cells, user can navigate dates but cannot switch variants on mobile, admin can select variant and filters, event details open correctly, local event data renders end-to-end, future CalDAV/Notion sources can map into same unified event model. | functional | must | All listed acceptance criteria pass end-to-end testing. | automated-test | FF4-SLICE-CALENDAR |
| FF4-CAL-022 | FF4 doc line 673 | Calendar / mobile view switcher removed | Mobile CalendarModule must not show variant switching UI; only time navigation remains | The mobile CalendarModule must not show any variant switching UI. Only time navigation (prev/next arrows, Today button) remains visible on mobile. The view switcher must be removed from the mobile Calendar. | UI | must | Given the Calendar renders on mobile, then no variant switcher is visible; Given the Calendar on mobile, then prev/next arrows and Today button are the only navigation controls. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-023 | FF4 doc line 674 | Calendar / auto-adapt small cells | When calendar cell width < 200px, auto-switch to Compact or Event List variant | When the calendar cell width is less than 200px, the Calendar must auto-switch to Compact or Event List variant. This prevents layout breakage in small cells. | functional | must | Given a Calendar cell width is 180px, then the Calendar auto-switches to Compact or Event List; Given the cell width increases above 200px, then the admin-selected variant is restored. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-024 | FF4 doc line 675 | Calendar / sourceType badges | Each event shows a colored badge based on event source: Local/Gray, CalDAV/Blue, Notion/Purple, Custom/Teal | Each event must show a colored badge indicating its source type: Local → Gray, CalDAV → Blue, Notion → Purple, Custom → Teal. The badge must be visible in the event display. | UI | must | Given an event with sourceType "local", then a gray badge is displayed; Given an event with sourceType "notion", then a purple badge is displayed. | manual-flow-test | FF4-SLICE-CALENDAR |
| FF4-CAL-025 | FF4 doc line 676 | Calendar / notes display | Events with notes field show up to 2 lines of notes text, truncated with numberOfLines | Events that have content in the `notes` field must show up to 2 lines of notes text, truncated with `numberOfLines` property. | UI | must | Given an event has notes content, then up to 2 lines of notes are displayed; Given long notes content, then text is truncated after 2 lines. | manual-flow-test | FF4-SLICE-CALENDAR |
| **Templates Requirements** |
| FF4-TPL-001 | FF4 doc lines 556-563 | Templates / functional rework | Most templates are not functional; need brainstorm, plan, and build functional templates; when coding, just send JSON | Templates need massive rework. Must brainstorm and plan functional templates and then build them. Template coding should only send JSON — a 2-step process: make backend complete, then have JSON ready to send with all functional features. If you can't just send JSON directly, you're doing something wrong. | workflow | must | Given a template is applied, then the JSON is sent to the backend and renders correctly on the frontend; Given all template features, then they work end-to-end when the JSON is applied. | automated-test | FF4-SLICE-TEMPLATES |
| FF4-TPL-002 | FF4 doc lines 31-32, 560-563 | Templates / no non-existent components | Some templates use non-existent components; must fix to use real components only | Templates must only use real, working, existing component types. No fake or placeholder components. | functional | must | Given any template is loaded, then all component types referenced are valid in the component registry; Given a template has a non-existent component type, then it is flagged during validation and not allowed to publish. | automated-test | FF4-SLICE-TEMPLATES |
| FF4-TPL-003 | FF4 doc lines 560-563 | Templates / two-step process | Backend fully complete → then code templates by sending JSON directly | The template workflow must be: 1) make backend fully complete with all functional features, 2) code templates by having JSON ready to send directly. This must be a two-step process where templates are just JSON payloads. | architecture | should | Given the backend is complete, then templates can be sent as pure JSON; Given a template JSON is sent, then it renders with all functional features. | automated-test | FF4-SLICE-TEMPLATES |
| FF4-TPL-004 | FF4 doc lines 1337-1361 | Delivery/Versioning / Template model | Templates are versioned sources; apply to module → Module Working Draft; never preview/publish to mobile directly; template versions have timestamped names; create checkpoint before applying to existing module | Templates are versioned sources that do NOT publish to mobile and do NOT preview on mobile directly. Template flow: Template Version → Apply to module → Module Working Draft → Web-admin module preview → Module Checkpoint/Version → App Editor reference → App Preview/Publish → Mobile. Template requirements: versions have timestamped names (renamable), applying to existing module creates checkpoint first, applying writes to Module Working Draft (not directly to live mobile), template version history shows where it was applied, existing modules from old templates do not auto-update when template changes. | workflow | must | Given a template is applied to an existing module, then a checkpoint is created first; Given a template is applied, then the module working draft is updated (not live mobile); Given a template version is created, then it has a timestamped name that can be renamed. | manual-flow-test | FF4-SLICE-TEMPLATES |
| FF4-TPL-005 | FF4 doc lines 1362-1378 | Delivery/Versioning / template apply modal | Template apply modal with version selector, target selector (new/existing module), checkpoint creation confirmation, Cancel/Apply buttons | The template apply modal must include: template version selector dropdown, target selector (Create new module OR Apply to existing module with module dropdown), checkpoint confirmation ("Create checkpoint of current module"), and Cancel/Apply buttons. | UI | must | Given the user clicks to apply a template, then the modal appears with version selector, target selector, checkpoint option, and Cancel/Apply buttons; Given the user selects "Apply to existing module", then a checkpoint is created before applying. | manual-flow-test | FF4-SLICE-TEMPLATES |
| FF4-TPL-006 | FF4 doc lines 1568-1576 | UI requirements / Template UI | Template UI: version selector, apply to new/existing module, auto-checkpoint before applying to existing module, no mobile publish controls | The Template UI must include: template version selector, apply to new module / existing module, auto-checkpoint before applying to existing module, no mobile publish controls. | UI | must | Given the Templates page is open, then version selector, apply options, and checkpoint controls are present; Given the Templates page, then mobile publish controls are absent. | manual-flow-test | FF4-SLICE-TEMPLATES |
| **Workflows Requirements** |
| FF4-WF-001 | FF4 doc lines 566-568 | Workflows | Workflows look like they're starting to be complete; need test workflows that can be seen and tested | The workflow system must include test workflows (e.g., Daily Summary, Event Reminder, New Todo Alert) that can be visually inspected and tested. Users must be able to see and test workflows themselves. | QA | must | Given the Workflows page is open, then sample test workflows are visible; Given a test workflow is run, then it executes correctly and produces visible results. | manual-flow-test | FF4-SLICE-WORKFLOWS |
| FF4-WF-002 | FF4 doc lines 566-568 | Workflows / test workflows detail | Need sample workflows: Daily Summary (9am notification with today's events), Event Reminder (15 min before calendar events), New Todo Alert (notifies when todo added) | The system must include test workflows: "Daily Summary" (9am notification with today's events), "Event Reminder" (15 min before calendar events), "New Todo Alert" (notifies when todo added). These must be seeded as sample data. | data | should | Given the workflows page is open, then Daily Summary, Event Reminder, and New Todo Alert workflows exist; Given a workflow is triggered, then it performs its intended action. | automated-test | FF4-SLICE-WORKFLOWS |
| **Versioning Requirements** |
| FF4-VER-001 | FF4 doc lines 586-612, 617-628 | Delivery/Versioning / terminology | Use exact terminology: Working Draft, Checkpoint, Version, Live Version, Preview Session, Publish, Restore, Pin, Use newest | The following terminology must be used in UI and code: **Working Draft** (mutable autosaved state being edited), **Checkpoint** (immutable saved snapshot from working draft), **Version** (immutable named/timestamped snapshot), **Live Version** (currently published app version assigned to mobile device), **Preview Session** (temporary render of draft/version), **Publish** (promote App Editor draft/version into live version), **Restore** (copy previous version into working draft, does not auto-publish), **Pin** (make app reference one specific module version), **Use newest** (App Editor chooses newest available module version). Must NOT use: draft v1, approve, reject. | terminology | must | Given the UI, code comments, and API docs, then all versioning concepts use the specified terminology; Given the UI, then "Draft v1", "Approve", "Reject" are absent from the versioning flow. | review-only | FF4-SLICE-VERSIONING |
| FF4-VER-002 | FF4 doc lines 630-653 | Delivery/Versioning / version naming | Default timestamp-based names (e.g. "2026-05-13 09:44"); user can rename; required fields: default timestamp name, custom display name, created time, source, status, parent version, used by, validation status, change summary | Version/checkpoint naming rules: MUST NOT use v1/v2/v3 as visible names. Default name format: `2026-05-13 09:44` (local timezone, Asia/Shanghai unless user changes). User can rename with optional custom name suffix (e.g., "2026-05-13 09:44 — Home module layout checkpoint"). Required fields for every visible version/checkpoint: Default timestamp name (auto-generated), Custom display name (optional user-renamable), Created time (exact timestamp), Source (App Editor/Module Editor/Template Editor/AI agent/manual restore/template apply/import), Status (working draft/checkpoint/published/live/archived/restored source/previewed/failed validation), Parent version/checkpoint, Used by (apps/devices/templates/modules), Validation status (valid/warning/invalid/not checked), Change summary (short optional text, auto-generated if possible, user-editable). | data | must | Given a checkpoint is created, then its default name is a timestamp in format "YYYY-MM-DD HH:MM"; Given the user renames a version, then the custom name is shown alongside the timestamp; Given a version is displayed, then all required fields are populated. | manual-flow-test | FF4-SLICE-VERSIONING |
| FF4-VER-003 | FF4 doc lines 655-692 | Delivery/Versioning / version tree | Version history as timestamped tree, not flat v1/v2/v3 list; parent-child relationship, immutable snapshots | Version history must be displayed as a timestamped tree, not a flat v1/v2/v3 list. Must show parent-child relationships. Example: Module tree showing branches like `├── 2026-05-13 09:44 — Current working draft`, `│ ├── 2026-05-13 09:35 — Added compact calendar`, etc. Tree must clearly show: immutable snapshots, parent-child relationship, restore capability, compare, rename, and clear display of what is live/used. Full Git branching is not needed in v1. | UI | must | Given version history is opened, then versions are displayed as a tree with parent-child relationships; Given a version is live/used, then it is clearly marked; Given a version is the current working draft, then it is clearly marked. | manual-flow-test | FF4-SLICE-VERSIONING |
| FF4-VER-004 | FF4 doc lines 695-856 | Delivery/Versioning / entity model | Conceptual entities: App, AppWorkingDraft, AppVersion, Module, ModuleWorkingDraft, ModuleVersion, Template, TemplateVersion, Device, PreviewSession | The system must implement the following entity model: **App** (id, name, icon, description, current_working_draft_id, current_published_version_id, created/updated/deleted_at; includes app-level config: name, icon, splash, theme, design tokens, dark mode, default launch module, bottom bar, Launchpad, module membership, device assignments, module references and version selection policy), **AppWorkingDraft** (id, app_id, config_json, module_references, last_autosaved_at, validation_status, validation_errors, base_version_id, dirty), **AppVersion** (immutable published/checkpoint snapshot; stores resolved module versions, module reference policies, schema_version, min_mobile_runtime_version, created_at, created_by, source, parent_version_id, status, validation_result, change_summary; published version stores concrete module version at publish time even if "Use newest"), **Module** (id, name, icon, description, enabled_default, current_working_draft_id, current_version_id), **ModuleWorkingDraft** (id, module_id, sdui_json, last_autosaved_at, base_version_id, validation_status, validation_errors, dirty), **ModuleVersion** (id, module_id, display_name, default_timestamp_name, custom_name, sdui_json, schema_version, required_component_types, required_action_types, required_data_bindings, created_at, source, parent_version_id, validation_result, change_summary), **Template** (id, name, description, category, current_version_id), **TemplateVersion** (id, template_id, display_name, default_timestamp_name, custom_name, template_json, created_at, source, parent_version_id, validation_result, change_summary), **Device** (id, device_name, device_type, platform, installed_runtime_version, supported_schema_versions, assigned_app_id, active_app_version_id, last_seen_at, connection_status, update_status, preview_session_id), **PreviewSession** (id, target_type (web_admin/mobile_device), device_id, app_id, app_draft_id/app_version_id, resolved_config_json, resolved_module_versions, created_at, expires_at, status (active/expired/exited/failed), validation_result). | data | must | Given the backend data model, then all listed entities exist with the specified fields. | review-only | FF4-SLICE-VERSIONING |
| FF4-VER-005 | FF4 doc lines 770-772 | Delivery/Versioning / published version snapshot | Published App Version must store reproducible snapshot of module versions used at publish time, even for "Use newest" references | When an App Version is published, it must store a reproducible snapshot of what module versions were used at publish time. Even if an App Editor reference says "Use newest", the published App Version must record which concrete module version was resolved. This prevents old published app versions from changing unexpectedly. | data | must | Given an App Version is published with "Use newest" for a module, then the stored version includes the concrete resolved module version ID. | automated-test | FF4-SLICE-VERSIONING |
| FF4-VER-006 | FF4 doc lines 1381-1447 | Delivery/Versioning / validation | Validation at multiple stages: autosave (lightweight: JSON parseable, required fields, no catastrophic shape), checkpoint/version (schema valid, component types exist, props match schema, bindings present, actions valid, server actions exist, variables valid), app preview (config valid, bottom bar ≤5 slots, Launchpad references enabled modules, default module exists, all module references resolve, Use newest/specific valid, theme/dark mode tokens valid, device runtime compat), publish (all preview checks + devices can render components/schema versions, no unknown component types, no fake/template-only types, no deleted modules, no disabled module in bottom bar, metadata present, runtime compat, snapshot reproducible) | Validation must happen at multiple stages. Autosave: lightweight (JSON parseable, required high-level fields, no catastrophic invalid shape, allow incomplete drafts with warnings). Checkpoint/version: SDUI schema valid, rows/cells/components valid, component type exists in registry, props match schema, required bindings present or have fallbacks, actions reference valid types, server actions exist, variables valid. App preview: app config valid, bottom bar ≤ 5 slots, Launchpad references enabled modules only, default launch module exists, all module references resolve, "Use newest" has newest valid version, "Use specific" has selected version, theme/dark mode tokens valid, device runtime compat if mobile preview. Publish: all preview checks plus all selected devices can render required schema/component versions, no unknown component types, no fake template-only types, no deleted modules, no disabled module in bottom bar, required app metadata present, mobile runtime compatibility passes, publish snapshot can be reproduced. If publish validation fails, do NOT publish — show exact errors with location. | validation | must | Given autosave occurs, then lightweight validation runs and warnings are shown; Given a checkpoint is created, then full validation runs and blocks if invalid; Given publish is attempted with invalid state, then publish is refused with exact errors pointing to Module → Row → Cell → Component. | automated-test | FF4-SLICE-VERSIONING |
| FF4-VER-007 | FF4 doc lines 1435-1447 | Delivery/Versioning / publish validation errors | Publish errors must show exact location: Module → Row → Cell → Component; show error details and fix guidance | When publish validation fails, the error message must include the exact location: "Home Module → Row 3 → Cell 1: Unknown component type 'todo'. This component is used by Home Module version '2026-05-13 09:44'. Fix: register Todo as a real component, or replace it with a supported component." | validation | must | Given publish fails with an invalid component type, then the error includes: module name, row number, cell number, component type, version that uses it, and fix guidance. | manual-flow-test | FF4-SLICE-VERSIONING |
| FF4-VER-008 | FF4 doc lines 1556-1566 | UI requirements / Version History UI | Version History UI: tree view, timestamp default name, custom user-renamable name, status badges (Working Draft/Checkpoint/Live/Used by App/Used by Device/Archived), source badges (App Editor/Module Editor/Template/AI/Restore/Import), actions (Rename/Restore to Draft/Compare/Archive/View JSON), used-by panel | The Version History UI must include: tree view, timestamp default name, custom user-renamable name, status badges (Working Draft, Checkpoint, Live, Used by App, Used by Device, Archived), source badges (App Editor, Module Editor, Template, AI, Restore, Import), actions (Rename, Restore to Draft, Compare, Archive, View JSON), used-by panel. | UI | must | Given the Version History is opened, then the tree view with all listed elements is visible; Given a version is selected, then Rename, Restore to Draft, Compare, Archive, and View JSON actions are available. | manual-flow-test | FF4-SLICE-VERSIONING |
| FF4-VER-009 | FF4 doc lines 664-689 | Delivery/Versioning / version compare/diff | Version comparison showing diff between two versions: row counts, component counts, component type lists (added vs removed), side-by-side comparison UI | The version comparison UI must show: row counts comparison, component counts, component type lists (added vs removed), side-by-side view. Available as "Compare Versions" button when ≥2 versions exist. User selects two versions (A and B). Must work in both Module Editor and App Editor. | UI | should | Given ≥2 module versions exist, then "Compare Versions" button is available; Given two versions are selected, then side-by-side diff shows row counts, component counts, added/removed types. | manual-flow-test | FF4-SLICE-VERSIONING |
| **Backend Requirements** |
| FF4-BE-001 | FF4 doc lines 1664-1737 | Backend / Deployment Plan | One Helm server process, one exposed port, one Docker deployment; backend serves both API and admin interface | The deployment goal: bundle backend and Helm Admin for production. One Helm server process, one exposed port, one Docker deployment. Normal API/mobile/app traffic to backend; browser access to server root opens Helm Admin. Target: `server:8000/` → Helm Admin, `server:8000/admin` → Helm Admin, `server:8000/api/...` → Backend API, `server:8000/ws/...` → Realtime/mobile updates, `server:8000/mcp/...` → MCP/agent endpoints. The backend must own the public production port. API routes must be clearly namespaced to not collide with admin routes. Opening server root in browser must lead to Helm Admin. | architecture | must | Given the production deployment, then one Docker container serves both backend API and admin UI on a single port; Given a browser visits server root, then it opens Helm Admin; Given mobile/app clients access API, then API routes respond correctly alongside admin routes. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-002 | FF4 doc lines 1689-1700 | Backend / Deployment / inspect-first | Do not blindly implement as quick routing hack; inspect actual codebase first then produce plan | Before implementing bundled deployment, the agent must inspect: current codebase structure, route layout, frontend build system, backend entrypoint, Docker setup, and development workflow. Then produce an implementation plan that fits the existing architecture. | architecture | must | Given the deployment implementation, then it fits the existing codebase structure and does not break existing routes. | review-only | FF4-SLICE-BACKEND |
| FF4-BE-003 | FF4 doc lines 1702-1713 | Backend / Deployment / dev vs production | Development keeps separate dev servers; production uses bundled model; do not make dev worse to force production model | Development can keep separate dev servers for hot reload and debugging. Production should prefer bundled model for simple self-hosting. Must not make local development worse just to force the production model. | architecture | must | Given development mode, then hot reload and debugging still work; Given production mode, then bundled single-port deployment works. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-004 | FF4 doc lines 1708-1713 | Backend / Deployment / Docker | Single Helm service container; one main port; persistent data via volumes/env files; hide complexity behind Docker Compose if needed | The production Docker setup must: aim for a single Helm service, expose one main port for backend/admin bundle, handle persistent data/db files/logs/config via volumes/env files, hide complexity behind Docker Compose only if separate services are needed internally. | architecture | must | Given `docker compose up`, then the Helm service starts on a single exposed port with data persistence. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-005 | FF4 doc lines 1728-1737 | Backend / Deployment / acceptance criteria | One deployment runs backend + admin together; single port exposed; server root opens admin; API/WebSocket/MCP/sync still work; admin routing works after refresh; dev hot reload works; Docker simple enough for self-hosted user; no hardcoded assumptions | Acceptance criteria: One production deployment runs Helm backend + Helm Admin together, only one main port exposed, visiting server root opens Helm Admin, API/WebSocket/MCP/mobile sync/publish/preview still work, admin frontend routing works after browser refresh on nested admin pages, development hot reload remains possible, Docker deployment simple enough for self-hosted user, implementation does not hardcode assumptions conflicting with existing codebase. | architecture | must | All listed acceptance criteria pass. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-006 | FF4 doc lines 1739-1747 | Backend / Deployment / fallback | Clean fallback path: separate frontend service, separate API service, reverse proxy later; don't make separation impossible | If bundled model causes problems, maintain clean fallback: run Helm Admin as separate frontend service again, keep backend API service separate, optionally place reverse proxy in front. Must not design bundled deployment in a way that makes separating admin/backend impossible. | architecture | should | Given the bundled deployment, then it is possible to separate admin and backend services again. | review-only | FF4-SLICE-BACKEND |
| FF4-BE-007 | FF4 doc lines 1450-1521 | Backend / API surface | Required API endpoints for Apps, Modules, Templates, Devices, Preview Sessions | The following API capabilities must exist: **Apps**: GET/POST /apps, GET /apps/:appId, PATCH /apps/:appId/draft, GET /apps/:appId/draft, POST /apps/:appId/checkpoints, GET /apps/:appId/versions, GET /apps/:appId/versions/:versionId, PATCH rename, POST restore-to-draft, POST preview/web, POST preview/device, POST publish. **Modules**: GET/POST /modules, GET /modules/:moduleId, PATCH /modules/:moduleId/draft, GET /modules/:moduleId/draft, POST /modules/:moduleId/checkpoints, GET versions, GET /:versionId, PATCH rename, POST restore-to-draft, POST preview/web, GET usage. No POST /modules/:moduleId/preview/device (device preview belongs to App Editor). **Templates**: GET/POST /templates, GET /templates/:templateId, GET /templates/:templateId/versions, POST /templates/:templateId/versions, PATCH rename, POST apply. **Devices**: GET/POST /devices, POST register, GET /devices/:deviceId, PUT /devices/:deviceId/app, GET status, POST exit-preview. **Preview Sessions**: GET /preview-sessions/:sessionId, POST exit, POST extend. | architecture | must | Given the backend is running, then all listed API endpoints exist and respond correctly. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-008 | FF4 doc lines 684-692, 694-699 | Backend / data models | New models: Note, ModuleWorkingDraft, ModuleVersion, PreviewSession; enhanced: apps (draft_id, published_version_id), module_instances (draft_id, version_id), devices (app_id, app_version_id, preview_session_id, runtime_version, schema_versions, update_status), sdui_templates (version_id) | New data models: Note (id, user_id, title, content), ModuleWorkingDraft (id, module_id, user_id, sdui_json, last_autosaved_at, base_version_id, validation_status, validation_errors, dirty), ModuleVersion (id, module_id, user_id, version_number, display_name, default_timestamp_name, custom_name, sdui_json, source, parent_version_id, change_summary, validation_status, validation_errors, schema_version, created_at), PreviewSession (id, user_id, target_type, app_id, module_id, resolved_config_json, resolved_sdui_json, device_id, status, created_at, expires_at, exited_at). Enhanced: apps (current_working_draft_id, current_published_version_id), module_instances (current_working_draft_id, current_version_id), devices (assigned_app_id, active_app_version_id, preview_session_id, installed_runtime_version, supported_schema_versions, update_status), sdui_templates (current_version_id). | data | must | Given the database schema, then all new and enhanced models exist with specified fields. | review-only | FF4-SLICE-BACKEND |
| FF4-BE-009 | FF4 doc lines 706-734 | Backend / versioning endpoints | Full implementation: module draft endpoints, checkpoint/version CRUD, restore, rename, preview, usage; app draft/checkpoint/version/publish endpoints; template version endpoints; preview session CRUD; device status | The following versioning endpoints must be implemented: PATCH /api/modules/{id}/draft (autosave), GET /api/modules/{id}/draft, POST /api/modules/{id}/checkpoints, GET /api/modules/{id}/versions, GET /api/modules/{id}/versions/{vid}, PATCH rename, POST restore-to-draft, POST preview/web, GET usage. Similarly for apps: PATCH /api/apps/{id}/draft, GET, POST checkpoints, GET versions, POST restore-to-draft, POST preview/web, POST preview/device, POST publish. Templates: GET/POST /api/templates/{id}/versions, POST apply. Preview sessions: GET /api/preview-sessions/{id}, POST exit, POST extend. Devices: GET /api/devices/{id}/status, POST exit-preview. | architecture | must | Given the backend is running, then all listed versioning endpoints exist with correct request/response schemas. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-010 | FF4 doc lines 736-741 | Backend / validation service | validation_service.py validates SDUI screens at autosave, checkpoint/version, app preview, publish stages | A validation_service.py must exist that validates SDUI screens at multiple stages: autosave (lightweight schema check), checkpoint/version (component types, actions, data bindings), app preview (bottom bar slots, Launchpad, modules), publish (device compatibility, runtime version check). | architecture | must | Given a module is autosaved, then lightweight validation runs; Given a checkpoint is created, then full validation runs; Given publish is attempted, then device compatibility validation runs. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-011 | FF4 doc lines 561-562, 762-764 | Backend / sample data seeding | Sample data: workflows (Daily Summary, Event Reminder, New Todo Alert), variables (user.name, app.theme, greeting.morning), calendar events (demo events) | The backend must seed sample data including: Workflows — "Daily Summary" (9am notification), "Event Reminder" (15 min before calendar events), "New Todo Alert" (notifies when todo added); Variables — user.name, app.theme, greeting.morning; Calendar events — sample events for demo. | data | should | Given the backend starts with seed data, then sample workflows, variables, and calendar events exist. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-012 | FF4 doc lines 770-771 | Backend / template seed validation | All template seed screen_json payloads validated against validate_sdui_screen_payload() at startup | All seed template screen_json payloads must be validated against validate_sdui_screen_payload() at startup. Validation errors logged as warnings. Templates rewritten to use only real, working component types. | validation | must | Given the backend starts up, then all seed templates are validated; Given a template has an invalid component type, then it is logged as a warning. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-013 | FF4 doc lines 766-768 | Backend / screen history migration | Existing sdui_screen_history table deprecated in favor of module_versions; new code writes to module_versions only | The existing sdui_screen_history table must be deprecated. New code must write to module_versions only. Old table and APIs remain for backward compatibility with existing data. | architecture | must | Given new versioning code runs, then it writes to module_versions, not sdui_screen_history; Given old data exists, then old APIs still function for backward compatibility. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-014 | FF4 doc lines 684-689 | Backend / cleanup service | Cleanup endpoints: GET /api/admin/cleanup/preview shows test data counts; POST /api/admin/cleanup/execute removes test apps/modules/templates (names starting with "test"/"Test") | Cleanup endpoints must exist: GET /api/admin/cleanup/preview returns counts of test data that would be deleted (apps, module instances, templates with names starting with "test"/"Test"). POST /api/admin/cleanup/execute performs the actual deletion and returns deletion counts. | functional | must | Given test apps/modules/templates exist, then GET preview returns their counts; Given POST execute is called, then test data is removed and deletion counts are returned. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-015 | FF4 doc lines 615-616 | Delivery/Versioning / canonical flow | Canonical publishing flow: Module Editor → working draft/checkpoints → web preview → App Editor → App version → publish → mobile | The canonical flow must be: Module Editor edits module content → autosaved module working draft/checkpoints → web admin module preview only → module version becomes available to App Editor → App Editor chooses module versions and app-level settings → full app preview in web admin OR temporary preview on mobile device → publish from App Editor → mobile device renders published App Version. | workflow | must | Given the full editing-to-publish flow, then the module editor never pushes directly to mobile; Given the flow is followed, then mobile only updates after App Editor publish. | manual-flow-test | FF4-SLICE-BACKEND |
| FF4-BE-016 | FF4 doc lines 1295-1333 | Backend / WebSocket events | New WebSocket events: preview_session_started (mobile enters preview), app_version_published (new version available) | The backend must send these new WebSocket events: `preview_session_started` (session_id, app_config?, sdui_json?) — mobile enters preview mode with preview config; `app_version_published` (version_id, app_id) — new app version published to mobile devices. WebSocket is notification only; device fetches via REST. | architecture | must | Given a preview session is created, then preview_session_started is sent via WebSocket; Given an app version is published, then app_version_published is sent via WebSocket. | automated-test | FF4-SLICE-BACKEND |
| FF4-BE-017 | FF4 doc lines 751-753 | Backend / MCP tools / FF4 | New MCP tools: helm_create_checkpoint, helm_list_module_versions, helm_restore_version, helm_publish_version | The MCP server must support these new tools: helm_create_checkpoint (module_id, change_summary?), helm_list_module_versions (module_id, limit?, offset?), helm_restore_version (module_id, version_id), helm_publish_version (module_id, version_id?). | architecture | must | Given the MCP server is running, then helm_create_checkpoint, helm_list_module_versions, helm_restore_version, and helm_publish_version tools are available. | automated-test | FF4-SLICE-BACKEND |
| **MCP Requirements** |
| FF4-MCP-001 | FF4 doc lines 1748-1750 | MCP/agents | MCP must be made complete; integrate into QA system; make everything AI-ready; prepare for sub-agents to create frontend apps via MCP | The MCP system must be completed. Must be integrated into the QA system. Everything must be made AI-ready. The MCP must support sub-agents creating frontend apps directly. | architecture | must | Given the MCP server is running, then AI agents can create frontend apps via MCP tools; Given QA integration, then MCP tools are covered by QA tests. | automated-test | FF4-SLICE-MCP-QA |
| FF4-MCP-002 | FF4 doc lines 1748-1750 | MCP/agents / AI-ready | AI must be able to connect to MCP and test it, finding flaws | The MCP must be set up so that Barry can directly connect the AI to the MCP to test it and find flaws. The MCP must support summoning sub-agents that can complete MCP tasks and create frontend apps. | functional | must | Given an AI connects to the MCP, then it can use all MCP tools to create and modify frontend apps; Given sub-agents are summoned, then they can use MCP tools independently. | automated-test | FF4-SLICE-MCP-QA |
| **QA/Testing Requirements** |
| FF4-QA-001 | FF4 doc lines 202-203 | Variable system / QA | Variable system must be tested with a better QA script | The variable system must have comprehensive QA coverage. A better QA script must ensure end-to-end variable integration testing. | QA | must | Given QA tests for variables, then they cover variable insertion, display, rendering, and edge cases across text, markdown, and component properties. | automated-test | FF4-SLICE-MCP-QA |
| FF4-QA-002 | FF4 doc lines 15-16 | App Editor / live testing | Live testing not done correctly; most things not functional | Live testing must be performed correctly to verify that App Editor features are functional. Dark mode sync and other settings must be verified end-to-end. | QA | must | Given a feature is implemented, then live testing verifies it works on the actual target (mobile app, web admin). | manual-flow-test | FF4-SLICE-MCP-QA |
| FF4-QA-003 | FF4 doc lines 3-4, 1625-1628 | Testing / cleanup | Testing must clean up after itself; use main sample/template or create temporary entities with clear names and delete after; no leftover Test App/New App/Test Module junk | Testing must clean up after itself. Tests must not leave random "Test App", "New App", "Test Module", or junk templates in the editor. Tests should use the main sample/template or create temporary entities with clear names and delete them after test completion. | QA | must | Given a test suite runs, then no test data remains in the editor; Given test entities are created, then they have clear names and are deleted upon test completion. | automated-test | FF4-SLICE-MCP-QA |
| FF4-QA-004 | FF4 doc lines 1753-1754 | Others / react-doctor | Integrate react-doctor into the Reviewer agent | The react-doctor tool (https://github.com/millionco/react-doctor) must be integrated as part of the Reviewer agent workflow. It must be used to detect expensive renders, unnecessary re-renders, and React performance regressions. | QA | should | Given the Reviewer agent runs, then react-doctor diagnostics are included in the review. | automated-test | FF4-SLICE-MCP-QA |
| FF4-QA-005 | FF4 doc lines 188-189 | Visual Editor / bottom divider / app preview | Bottom divider functionality cannot be verified because app preview is broken; app preview needs full rewrite | The app preview must be functional to enable testing of layout features like bottom dividers. The app preview needs a full rewrite to make it work. | QA | must | Given the app preview renders, then bottom divider settings and other layout features are visible and testable. | manual-flow-test | FF4-SLICE-MCP-QA |
| FF4-QA-006 | FF4 doc lines 574-576 | Connections / usage | Connections look good but unclear how they are used directly in the program | Documentation and test cases must exist showing how Connections are used in the program. Users must be able to understand the end-to-end flow for connections. | docs | should | Given documentation exists, then it explains how connections are used end-to-end in the program. | review-only | FF4-SLICE-MCP-QA |
| FF4-QA-007 | FF4 doc lines 578-580 | Logs | Logs look like they have no issues; can be used more | The Logs system must continue to function without issues. It can be leveraged more heavily for debugging and monitoring. | QA | should | Given the Logs page is open, then logs display correctly and can be filtered. | manual-flow-test | FF4-SLICE-MCP-QA |
| FF4-QA-008 | FF4 doc lines 582-584 | Settings / admin panels | Admin panels show registered devices incorrectly; only frontend devices count | The Settings page's registered devices list must correctly show only frontend devices, not admin panels. The current display is not intentional and must be corrected. | functional | must | Given the Settings page shows registered devices, then only frontend/mobile devices are listed (not admin web panels). | manual-flow-test | FF4-SLICE-MCP-QA |
| **Notes Requirements** |
| FF4-NOTES-001 | FF4 doc lines 511-546 | Visual Editor / Notes | Notes is a real first-class local-first document/feed component; shows list of notes with title, preview, author icon, timestamp; tap opens full-page reading view; supports markdown rendering; user notes editable via dual view/edit model; AI notes read-only with author icons | The Notes Component must be a real first-class local-first document/feed component (not fake static template content). Must show a list/feed of notes with: title, preview text, author icon, and timestamp. Tapping a note opens a full-page reading view. Notes support rich formatting through Markdown rendering. User-created notes are editable through dual view/edit Markdown source model. AI-created notes are read-only and visually distinguished with author icons. | functional | must | Given the Notes component renders, then it shows a list of notes with title, preview, author icon, and timestamp; Given a note is tapped, then a full-page reading view opens; Given an AI-created note, then it is read-only with author icon. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-NOTES-002 | FF4 doc lines 530-535 | Visual Editor / Notes / data model | Local-first SQLite notes table; work locally before remote sync; component reads/writes through backend API, not fake template data | Notes must use a local-first SQLite notes table as primary source of truth. Notes must work locally before any remote sync exists. Remote connectors can be added later (Notion pages, Apple Notes, other services). The component must read/write through the backend/local notes API instead of relying on fake template data. | data | must | Given the Notes component renders, then it fetches data from the backend notes API; Given no backend is available, then local SQLite data is used. | automated-test | FF4-SLICE-COMPONENTS |
| FF4-NOTES-003 | FF4 doc lines 537-541 | Visual Editor / Notes / template usage | Home/Personal Dashboard: Notes shows most recent note; Daily Planner: Notes shows today's notes inside Empty Container; "+ New Note" buttons call real notes.create server action | Notes must be integrated into templates: Home/Personal Dashboard — Notes component shows the most recent note or quick note, Daily Planner — Notes component shows today's notes inside vertical Empty Container alongside Calendar and Todo, Template buttons such as "+ New Note" should call the real notes.create server action. | workflow | should | Given the Home template, then the most recent note is displayed; Given the Daily Planner template, then today's notes appear in the Empty Container; Given a "+ New Note" button, then it calls notes.create. | manual-flow-test | FF4-SLICE-COMPONENTS |
| FF4-NOTES-004 | FF4 doc lines 543-546 | Visual Editor / Notes / V1 priority | V1 priority: backend binding, real data rendering, template integration; defer advanced features | V1 priority for Notes: correct backend binding, real data rendering, template integration. Do NOT spend V1 effort on: Notion sync, Apple Notes sync, image upload, collaboration, folders, tags, SDUI-rendered interactive notes. | functional | should | Given V1 Notes, then backend CRUD works and real data renders; Given V1 Notes, then advanced features (Notion sync, Apple Notes sync, etc.) are not implemented. | review-only | FF4-SLICE-COMPONENTS |
| **Input Bar Requirements** |
| FF4-IB-001 | FF4 doc lines 548-550 | Visual Editor / Input bar | Input bar must send action to backend server with input bar content when clicking send; requires server backend mechanisms, workflows, and variables properly bound | The Input Bar must function by sending an action to the backend server including the content of the input bar when the send button is clicked. This requires server backend mechanisms (workflows and variables) to be properly bound and working. | functional | must | Given text is entered in the Input Bar, when the send button is clicked, then the content is sent to the backend as a server_action; Given variables are referenced in the input, then they resolve correctly. | manual-flow-test | FF4-SLICE-COMPONENTS |
| **Todo/Article Card/Rich Text Requirements** |
| FF4-TODO-001 | FF4 doc lines 552-554 | Visual Editor / Todo, Article Card, Rich Text | Make these functional with what's in the templates; build templates from scratch; todo list and article cards must actually work on mobile | The TodoModule, ArticleCardModule, and RichTextRenderer components must be made functional according to template usage. Must build templates from scratch. Todo lists must let you add, toggle, and delete items via backend. Article cards must show real article data with navigation to detail. Rich text must render markdown content correctly. These must actually work on mobile, not just look good. | functional | must | Given a Todo component renders, then items can be added, toggled, and deleted via backend API; Given an ArticleCard renders, then it displays real data with tap navigation; Given RichText renders, then markdown content is displayed correctly. | automated-test | FF4-SLICE-COMPONENTS |
| FF4-TODO-002 | FF4 doc lines 552-554 | Visual Editor / don't make custom components | Don't make custom components; use the standard component system | All functional components (Todo, ArticleCard, RichText) must be built using the standard component system, not as custom/one-off components. | architecture | must | Given Todo, ArticleCard, and RichText components, then they are registered in the standard component registry and use standard SDUI patterns. | review-only | FF4-SLICE-COMPONENTS |
| **Architecture / Design Requirements** |
| FF4-DES-001 | FF4 doc lines 11-12 | App Editor / architecture | Always a JSON serving from backend; Helm admin modifies this JSON; frontend renders from it | The architecture must be: there is always a JSON serving from the backend. Helm admin is a way to modify this JSON that is being served to the frontend. The frontend renders from that JSON. | architecture | must | Given the backend is running, then it serves a JSON configuration for the frontend; Given the admin modifies settings, then the JSON served to the frontend is updated. | automated-test | FF4-SLICE-BACKEND |
| FF4-DES-002 | FF4 doc lines 507-509 | Visual Editor / Chat | Chat continues to be deferred | The Chat module continues to be deferred. No new Chat implementation is required in FF4. | functional | deferred | Deferred for future implementation. | deferred | FF4-SLICE-COMPONENTS |
| FF4-DES-003 | FF4 doc lines 578-580 | Others / Logs | Logs should be used more; looks like it has no issues | The Logs system can be used more extensively for debugging, monitoring, and troubleshooting. | docs | could | Given the Logs system, then it captures sufficient detail for debugging purposes. | review-only | FF4-SLICE-MCP-QA |
